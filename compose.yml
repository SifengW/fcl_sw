version: '3.1'

services:

  db:
    image: postgres
    restart: always
    environment:
      POSTGRES_PASSWORD: Aa123456

  adminer:
    image: adminer
    restart: always
    ports:
      - 5433:5433
